- **Technical design (`docs/technical/TECHNICAL_DESIGN.md`) — the implementation map.** Keep stable
  components, ownership, interfaces, trust boundaries, runtime topology, operations, and test strategy aligned
  with code and the PRD. Exact schemas/APIs/live state stay in executable or generated sources. Run
  `maintain-technical-design` after material architecture changes; do not load every linked topic doc by default.
  <!-- compounding-system: v6 — installed from claude_tools; run /compounding upgrade -->
