- **Compound continuously and close clean.** Fix obvious low-risk gaps inline; route larger or uncertain
  work to `docs/compounding/` with evidence and acceptance criteria. Before ending substantive work,
  reconcile affected product and technical docs and ensure every remaining follow-up is either completed,
  queued for an agent, or identified as a true operator-only action—never leave it only in chat prose.
- **Promote repeated workflows.** When a non-obvious multi-step procedure recurs or would save future
  rediscovery, move it into a repository-local skill and replace duplicated standing prose with a pointer.
  If it generalizes across repositories, use `promote-skill` to improve canonical AI Tools instead of
  vendoring a copy. Keep verified learnings in durable repository sources; memory is only a backup pointer.
  <!-- compounding-system: v7 — installed from claude_tools; run /compounding upgrade -->
