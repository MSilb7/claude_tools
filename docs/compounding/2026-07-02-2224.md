# Compounding — 2026-07-02 22:24 (docs-currency review session)

### [C1] Sync template pack to the evolved reference implementation (drain v2) — bump VERSION → 2
- **Severity:** medium
- **Effort:** medium
- **Pickup:** active-agent
- **Ready:** no
- **Files:** commands/compounding-templates/compounding-drain.md, commands/compounding-templates/SOP.md, commands/compounding-templates/claude-md-snippet.md, commands/compounding-templates/VERSION, docs/compounding/SOP.md, .claude/commands/compounding-drain.md, CLAUDE.md
- **Status:** OPEN

**Background:** During a docs-currency review (2026-07-02), the reference implementation
(`MSilb7/investment-agent`) was found to have evolved well past template pack v1: its drain worker
now runs **twice daily** (14:00/00:00 UTC), works **up to 3 items per fire** (was 1), and
**verifies declared `Ready-when:` machine-checkable gates** (flipping `Ready` with evidence);
its SOP has an Upstreaming section and diverges from the v1 template by ~124 diff lines.
investment-agent's SOP header says differences to either side become `Upstream: claude_tools`
items — but no upstream item had been filed. This entry is that item.

**Goal:** The template pack (v2) carries the drain-v2 behavior (multi-item cap, Ready-when gate
verification, twice-daily cadence guidance) generalized for any repo, and claude_tools' own
installed copies + CLAUDE.md bullet match it.

**Acceptance criteria:** (skeletal — refine before executing)
- [ ] Diff investment-agent's `docs/compounding/SOP.md` + `.claude/commands/compounding-drain.md`
      against the v1 templates; port every generalizable improvement (multi-item loop w/ cap,
      `Ready-when:` gates, Upstreaming section), keeping templates repo-agnostic (no bun/Turso/
      broker specifics — preserve the v1 generalizations, e.g. "no money-moving tools").
- [ ] `commands/compounding-templates/VERSION` → 2; version stamps in templates updated.
- [ ] claude_tools' own install (docs/compounding/SOP.md, .claude/commands/compounding-drain.md,
      CLAUDE.md snippet) re-synced via `/compounding upgrade` in the same PR (it currently says
      "daily … one item per fire").
- [ ] `/compounding upgrade` guidance still correct for downstream repos.
