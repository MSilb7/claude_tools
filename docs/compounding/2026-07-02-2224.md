# Compounding — 2026-07-02 22:24 (docs-currency review session)

### [C1] Sync the evolved bounded-drain behavior into the portable template pack
- **Severity:** medium
- **Effort:** medium
- **Pickup:** active-agent
- **Ready:** no
- **Files:** skills/compounding-drain/SKILL.md, skills/compounding/SKILL.md, commands/compounding-templates/compounding-drain.md, commands/compounding-templates/compounding-status.mjs, commands/compounding-templates/SOP.md, commands/compounding-templates/VERSION, docs/compounding/SOP.md, AGENTS.md
- **Status:** DONE (reviewed in PR #23; main landing PR #24)

**Background:** During a docs-currency review (2026-07-02), the reference implementation
(`MSilb7/investment-agent`) was found to have evolved well past template pack v1: its drain worker
now runs **twice daily** (14:00/00:00 UTC), works **up to 3 items per fire** (was 1), and
**verifies declared `Ready-when:` machine-checkable gates** (flipping `Ready` with evidence);
its SOP has an Upstreaming section and diverges from the v1 template by ~124 diff lines.
investment-agent's SOP header says differences to either side become `Upstream: claude_tools`
items — but no upstream item had been filed. This entry is that item.

**Goal:** The portable drain and template pack carry the bounded multi-item behavior, admissible
`Ready-when` gate verification, and provider-neutral cadence guidance for any repository.

**Refinement (2026-07-14):** The original acceptance criteria named template v2 and repository-local
Claude command files from the architecture that existed when this item was filed. The template pack
is now v7 and lifecycle workflow logic is portable, with `AGENTS.md` as the shared instruction
surface. This implementation therefore advances the pack to v8 and deliberately does not recreate
retired `.claude/commands/` or `CLAUDE.md` workflow copies.

**Acceptance criteria:**
- [x] Audit the current investment-agent default branch (`ebe6ecf`, 2026-07-14) and port its
      generalizable cap-three loop, `Ready-when` gates, and upstreaming behavior without its
      repository-specific Turso, broker, or destructive-reset details.
- [x] Expose `Ready-when` in the selector's structured output, with a held-in regression test that
      failed before the parser change and passes after it.
- [x] Keep cadence provider-neutral and repository-owned; twice daily remains an optional example
      for repositories with two meaningful eligibility windows.
- [x] Advance `commands/compounding-templates/VERSION` from 7 to 8 and update every canonical stamp.
- [x] Synchronize the canonical-owned installed SOP and shared AI Tools instructions while keeping
      portable skills authoritative instead of recreating legacy provider command copies.
- [x] Update `/compounding upgrade` guidance and portability checks for the bounded loop and gates.
