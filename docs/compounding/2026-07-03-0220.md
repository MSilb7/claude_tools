# Compounding — 2026-07-03 02:20 UTC

Source: end-session-review of the /compounding build (investment-agent thread) — install-integrity
agent findings. First items in this repo's own queue.

### [C-CT1] Fallback fetch list names a template that setup never installs
- **Severity:** low
- **Effort:** low
- **Pickup:** cleanup-routine
- **Ready:** yes
- **Files:** `commands/compounding.md` (§0)
- **Status:** DONE (PR #14)

**Background:** §0's cloud-fallback fetch list enumerates `compounding-status.test.mjs`, but no mode
installs the test into a target repo — it is template-CI-only. An off-repo session would fetch a
file it has no use for.

**Goal:** the fallback list matches what a session actually needs.

**Acceptance criteria:**
- [ ] §0 either drops `compounding-status.test.mjs` from the fetch list or annotates it
      "(template CI only — not installed into repos)"
- [ ] No template VERSION bump needed if only the skill body changes (stamps live in templates)

### [C-CT2] Ranking/ceiling rules restated in three template files — point at the selector instead
- **Severity:** low
- **Effort:** low
- **Pickup:** cleanup-routine
- **Ready:** yes
- **Files:** `commands/compounding-templates/SOP.md`, `commands/compounding-templates/compounding-drain.md`
- **Status:** OPEN

**Background:** severity-desc → effort-asc ranking and the `low` effort ceiling are encoded
authoritatively in `compounding-status.mjs` (`EFFORT_RANK`/`SEVERITY_RANK`/`COMPOUNDING_MAX_EFFORT`)
but restated as literals in the SOP's derived-states table + Auto-drain prose and in the drain
skill. A within-pack change (e.g. raising the ceiling) must touch all three or the prose lies —
exactly the duplication hazard the SOP itself warns about.

**Goal:** the selector is the single source of truth for ranking/ceiling; prose points at it.

**Acceptance criteria:**
- [ ] SOP + drain template reference "the selector's ranking (see compounding-status.mjs)" instead of
      restating the literals (keep ONE human-readable statement in the SOP table, marked as mirroring
      the selector)
- [ ] `VERSION` bumped to 2 in the same PR (template content change) so repos inherit via upgrade
