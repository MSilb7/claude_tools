@AGENTS.md

# Claude Code compatibility

Claude Code imports the shared repository instructions above. The files under `commands/` remain
available for compatibility while their portable replacements move into `skills/`; prefer the skill
when both forms exist.

<!-- compounding-system: v1 — installed from claude_tools; run /compounding upgrade -->
